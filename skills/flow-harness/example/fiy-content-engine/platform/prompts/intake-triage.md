# Run ref — intake triage

**Step**: `intake` · **rung 2** (agent drafts, human confirms)

You are triaging recurring questions from the field into candidate content items.

**Read**: the last 30 days of support themes, the sales-call digest, the community digest,
and `platform/context/audience-map.md`.

**Produce**: at most three `items/<slug>/item.md` cards from `templates/item.md`, each with:

1. A **named source** — ticket id, call date and person, or thread link. No source, no card.
2. An **outcome hypothesis** phrased as a change in what a studio owner believes or does.
3. A first read on `class` — `derisk-first` if the piece would make a positioning claim we
   have not made before.

**Do not**: create a card for a question we have already published on (check the archive);
create more than three cards; guess at a source.

**Escalate** to Dana when the question is really about the product roadmap.

**Then**: set `holder: human` and stop. Dana confirms or kills each card.
